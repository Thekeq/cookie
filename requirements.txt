aiogram==3.29.1
fastapi==0.135.1
pydantic==2.12.5
python-dotenv==1.2.2
uvicorn==0.51.0

# Общее состояние воркеров: лимитер запросов и владение фоновыми задачами.
# Нужен только при WEB_CONCURRENCY > 1 — без REDIS_URL код работает на
# in-process фолбэке, но и запускаться с несколькими воркерами откажется.
redis==8.1.0

# Драйвер PostgreSQL. Нужен, только если заполнен DATABASE_URL: на SQLite
# импорт даже не происходит. binary — колёса с уже собранным libpq, иначе на
# сервере понадобился бы компилятор и заголовки postgresql-dev.
psycopg[binary]>=3.2,<4

# Отправка трейсбеков. Нужен, только если заполнен SENTRY_DSN: без DSN пакет не
# импортируется вовсе. Метрики он не заменяет — их отдаёт /metrics своими
# силами (см. server/obs.py).
sentry-sdk>=2.20,<3

# Только для тестов: эмулятор Redis в процессе. Без него проверки redis-пути
# в test_platform.py пропускаются, а не падают.
fakeredis==2.37.0
